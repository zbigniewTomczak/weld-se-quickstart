Clone if you wold like to start using CDI implementation Weld with only Java SE. No bean container is required.

To run this project:
mvn package
mvn exec:java -Dexec.mainClass="org.jboss.weld.environment.se.StartMain"
